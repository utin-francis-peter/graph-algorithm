import { TAppState, TGameInterface } from "../../types/types";

const GameInterface: React.FC<TGameInterface> = ({ gameInputs }) => {
  return <div>GameInterface</div>;
};

export default GameInterface;
